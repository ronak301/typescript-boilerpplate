import { isSSR } from './isSSR';
import { isLocalhost } from './isLocalhost';
import { isProduction } from './isProduction';
import { getElement } from './getElement';

export { isSSR, isLocalhost, isProduction, getElement };
